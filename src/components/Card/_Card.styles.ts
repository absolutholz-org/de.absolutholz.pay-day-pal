import styled from "@emotion/styled";

export const Card = styled.div`
  background-color: var(--bg-primary);
  border-radius: 1rem;
  box-shadow: 0 4px 6px rgb(from var(--text-primary) r g b / 0.5);
`;
